const assert = require('assert');
describe('Devops-Demo prueba unitaria', () => {
 it('return 2', () => {
        assert.equal(1 + 1, 2);
    });
 it('return 9', () => {
        assert.equal(3 * 3, 9);
    });
});